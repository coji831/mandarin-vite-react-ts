# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo

# Terraform
**/.terraform/
.terraform.tfstate.lock.info
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*.local

# Terraform provider binaries (generated by terraform init)
terraform/.terraform/

# TypeScript incremental build info
*.tsbuildinfo

# CodeGraph data files
.codegraph/

# Environment files
.env
.env.local

# Verification artifacts (design docs, not source code)
verification-artifacts/

# WIP working docs (calibration/planning/research/tracking)
wip/
*storybook.log
storybook-static

# GCP service account keys

# Playwright MCP logs
.playwright-mcp/

# Build output
apps/frontend/build-output.txt

keys/
# Terraform state
*.tfstate
*.tfstate.backup

# External data (downloaded from Make Me a Hanzi) — root only
# Anchored (/data/) so docs/guides/data/ (seed-pipeline.md) is not shadowed.
/data/
# SOLAR-Ralph v5 governor runtime (solar-governor) — machine-local install artifacts.
# registry.json is the shared repo wiring and IS committed; config/state/ledger are per-machine.
.solar/config.json
.solar/state/
.solar/ledger.md
.solar/runs/
.solar/handoffs/
