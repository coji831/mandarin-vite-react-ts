1. fix layout in quiz page (DailyReviewTest) to keep it from jumping when new elements appears like AI response or gamification rewards (XP gain, badge awards)
2. Fix size and spacing, alignment of quiz page element
3. Hide/swap submit button with next button in the AI response to keep layout from jumping
4. Adjust submission flow UX to be more intuitive and less jarring when AI feedback is loading (e.g., disable submit button, show loading state, then swap to next button with feedback)
5. Fix sub navbar, item spacing, alignment, and active state styling
6. Adjust question display to prevent layout shifts when question types change (e.g., pinyin vs. character questions)
7. Adjust question display UX to help users understand the question type and what input is expected (e.g., placeholder text, icons, instructions)
8. Add hint button for meaning/pinyin of new words and adaptable for character construction hints later (e.g., 翻 is composed of 反 + 扁, so hint could show "Hint: 反 + 扁") when the basics knowledge feature is implemented in the future
9. Add tool tips for tone input to explain how to use numeric notation for tones (e.g., "Type pinyin with tone numbers: ma3 → mǎ")
10. Add support for word that have multiple readings/meanings (e.g., 行 can be xíng or háng) by showing all possible answers in the quiz and accepting any correct answer (e.g., "Acceptable answers: xíng, háng"), check current word data from csv file to update word thats have multiple readings/meanings and adjust quiz logic to handle multiple correct answers for those words (eg. 花（名）huā - flower, verb huā - to spend) should accept both "huā" and "huā" as correct answers, but show different meanings in the quiz question and feedback)
11. Add support to convert pinyin to characters so user don't have to change keyboard when answering pinyin questions (e.g., user types "ma3" and it converts to "吗" in the input field)
12. fix styling to make sure its aligned with theme of the app
13. Convert some components to use the common UI component library if possible to maintain consistency across the app and reduce duplicated code (e.g., buttons, inputs, progress bars)
14. Support generate quiz collection that related to user's interest (e.g., HSK level, topic, etc.) in the future by adding a filter option in the quiz settings and adjusting the quiz generation logic to take that into account
15. Add support to generate multi answer base on the correct answer but easy to be confused (e.g., for the word "妈 mā", generate distractors like "马 mǎ" and "麻 má" that are similar in tone or character to create more effective learning opportunities)
16. Change Prompt of AI feedback to focus on correct answer and shorter response (e.g., "Explain why learner confused [userAnswer] with [correctAnswer] for Chinese word [wordDetails]. Focus on tone/character/meaning differences. Keep it concise for mobile display.")
17. Change prompt of AI feedback to be clearer about what the user chose wrong. It is somehow confusing which answer was submitted by the user and have to guess
18. Fix Alignment in the result page to improve UX and keep the page from overflowing
19. Adapt the result page for word with multiple readings/meanings to show all possible meanings and readings in the results and feedback (e.g., for the word "行", show both "xíng - to walk" and "háng - line/row" in the quiz question, and if user answers "xíng", show feedback that they got the meaning correct but missed the alternative reading "háng")
20. Fix next review date display in the results page to show relative time (e.g., "Next review: in 3 days" instead of a specific date) to make it more user-friendly and easier to understand when the next review is due
21. add tooltips for lapse count
22. In results page use line style (eg. red color) to indicate wrong answer instead of an extra status column to make it more visually intuitive and less cluttered
23. Add support to retain last quiz results and a button to redo with same quiz or refresh with new quiz to help users review their mistakes and reinforce learning (e.g., after completing a quiz, show a "Review Mistakes" button that takes them to a quiz with only the questions they got wrong, and a "New Quiz" button that generates a new set of questions)
24. Review and clean up code to remove any unused variables, functions, or imports that may have been left over from previous iterations of the epic to improve code quality and maintainability
25. Review and update/add docs related to the learning retention features to ensure they are accurate and reflect the current state of the implementation, including any changes made during the development process (e.g., update acceptance criteria, business rules, related issues in the story docs)
26. (Do first) Rename the `/mandarin` folder to `/learning` to match with new scope change and update all related imports and references in the codebase to reflect this change
27. Add support for quiz without AI feedbacks, only response with correct answer and adaptability to plug with pre-generated feedback from database .
28. Adjust UI wording for lapse/leech indicators to be more user-friendly and less technical (e.g., "Struggling Words" instead of "Leeches", "Review Again" instead of "Lapse Count") to improve UX and reduce intimidation for beginner learners.
29. Ensure properties of type are matching by name and type between types thats are transferred between components and API calls to prevent type errors and improve code reliability (e.g., if API returns `nextReview` as a string, make sure to parse it to a Date object before using it in the quiz logic)
30. Make sure business rule for learning retention are clearly defined and mapped to its implementation to ensure the features are aligned with the learning science principles and effectively improve retention (e.g., interleaving, spaced repetition, desirable difficulty)
31. Fix dashboard layout to keep it in 1 screen and prevent it from overflowing when the quiz is integrated and shows the "Start Daily Review" button with due count badge (e.g., adjust spacing, font size, and layout of dashboard elements to accommodate the new quiz button and badge without causing overflow or layout issues)
32. The pinyin input need to remove spaces to be more user-friendly and match with common pinyin input methods (e.g., user types "ni hao" and it converts to "nǐhǎo" in the input field) and adjust the quiz logic to handle pinyin input without spaces for validation (e.g., when validating user answer, remove spaces from both user input and correct answer before comparing)
