/**
 * @file apps/backend/src/modules/readers/types/readers-audio.ts
 * @description Passage-audio wire shapes — promoted to @mandarin/shared-types (D5).
 * Thin re-export shim kept so existing importers stay untouched.
 */
export type {
  AudioSource,
  SentenceAudioResult,
  PassageAudioResponse,
} from "@mandarin/shared-types";
