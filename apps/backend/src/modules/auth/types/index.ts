/**
 * @file types/index.ts
 * @description Barrel exports for Auth module types
 *
 * Only re-exports — no inline definitions.
 */

export type { TokenPair, AuthResult, SanitizedUser, IJwtService, IPasswordService } from "./auth";
