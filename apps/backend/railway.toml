[build]
builder = "RAILPACK"
buildCommand = "npm install && npx prisma generate --schema=apps/backend/prisma/schema.prisma && npm run build --workspace=@mandarin/backend"

[deploy]
startCommand = "npm run start --workspace=@mandarin/backend"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"