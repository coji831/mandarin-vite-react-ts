const root = "/";

const mandarin_page = root + "mandarin";
const todo_page = root + "todo";
const auth_page = root + "auth";
const login_page = auth_page + "/login";
const register_page = auth_page + "/register";

export { root, mandarin_page, todo_page, auth_page, login_page, register_page };
