/**
 * types.ts
 * Phase 1 Gate Quiz — Engine types
 *
 * Re-exports strategy types from the main types barrel.
 * This file exists to maintain a clean engine/ subdomain boundary.
 */

export type { QuizStrategy, StrategyType, QuizQuestion, AnswerResult, QuizPhase } from "../types";
