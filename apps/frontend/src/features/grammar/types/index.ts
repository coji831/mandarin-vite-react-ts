/**
 * @file types/index.ts
 * @description Barrel exports for Grammar feature types.
 * Story 22.3: Grammar UI
 *
 * Only re-exports — no inline definitions.
 */

export type {
  GrammarSegment,
  GrammarSegmentEntityType,
  GrammarExample,
  GrammarRelatedPattern,
  GrammarPatternSummary,
  GrammarPatternDetail,
  GrammarListResponse,
  GrammarPatternData,
  GrammarFilter,
} from "./grammar";
