/**
 * @file utils/index.ts
 * @description Barrel exports for Grammar feature utils.
 * Story 22.3: Grammar UI
 *
 * Only re-exports — no inline definitions.
 */

export { mapGrammarApiToData, isPatternLocked, segmentToEntityRef } from "./grammarData";
